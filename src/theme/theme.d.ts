/**
 * Theme type declarations
 * Extends Material-UI theme types to include custom properties
 */

import '@mui/material/styles';

declare module '@mui/material/styles' {
  interface Theme {
    // Custom theme properties can be added here if needed in the future
  }
  interface ThemeOptions {
    // Custom theme options can be added here if needed in the future
  }
}
